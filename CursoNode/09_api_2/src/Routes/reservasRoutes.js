// import express from 'express';
// //Llego aquí con http://localhost:3000/api/clientes
// //Tengo que crear un ENRUTADOR (Router de express)
// const reservasRoutes =express.Router();

// //Todas las operaciones y rutas disponibles para los clientes

// reservasRoutes.get("/", getReservasHandler);
// reservasRoutes.post("/", postReservasHandler);
// reservasRoutes.delete("/:id", deleteReservasHandler);

// export default ReservasRoutes;

